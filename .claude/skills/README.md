# Repository Skills

These skills support repository collaboration workflows. They are governed by
the canonical rules in `AGENTS.md`.

- `analyze-issue/`: analyze GitHub issues before implementation.
- `analyze-pr/`: review pull requests with CI and diff evidence.
- `fix-issue/`: implement a scoped issue fix after analysis.

Generated review artifacts should be written under `.claude/reviews/`, which is
ignored by default.
