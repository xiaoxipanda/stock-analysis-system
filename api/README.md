# API Layer

Backend HTTP entrypoints live here. Future implementation can follow a versioned
layout, with `api/v1/endpoints` for route handlers and `api/v1/schemas` for
external request and response contracts.
